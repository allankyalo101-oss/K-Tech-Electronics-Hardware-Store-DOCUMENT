{
  "name": "your-project",
  "version": "1.0.0",
  "dependencies": {
    "gh-pages": "^6.3.0"
  },
  "homepage": "https://your-username.github.io/repo-name",
  "scripts": {
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  }
}